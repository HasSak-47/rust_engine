[package]
name    = "swiedowski_engine"
version = "1.0.0"
edition = "2021"

[workspace]
members = [
	"bindings",
	"math",
	"vulkan",
]

[dependencies]
binds  = {path = "bindings"}
math   = {path = "math"}
vulkan = {path = "vulkan"}
